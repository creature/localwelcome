<%= render partial: "shared/email_greeting", locals: { user: @user } %>

You requested an invite to <%= @event.title %>, an event ran by Local Welcome <%= @event.chapter.name %>. We'd love for you to come!
<% unless @event.email_info.blank? %>

  <%= @event.email_info %>
<% end %>

If you can make it, click the link below:
<%= accept_invitation_url(@token) %>

Or, if you can't make it, let us know via this link:
<%= decline_invitation_url(@token) %>

