<%= render partial: "shared/email_greeting", locals: { user: @user } %>

You requested an invite to <%= @event.name %>, an event ran by Local Welcome <%= @event.chapter.name %>. We'd love for you to come!
<% if @event.has_email_info? %>

<%= @event.optional_email_info_as_text %>
<% end %>

<%= render partial: "events/venue_info", locals: { event: @event } %>
If you can make it, click the link below:
<%= accept_invitation_url(@token) %>

Or, if you can't make it, let us know via this link:
<%= decline_invitation_url(@token) %>

