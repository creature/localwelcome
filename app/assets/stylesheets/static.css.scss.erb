.overview {
  background: url(<%= image_url("coffeecups.jpg") %>) no-repeat center/cover;
  margin-top: $nav-margin * -1;
  min-height: 200px;

  p {
    margin: 100px 0;
    font-size: 200%;
    background: rgba(255, 255, 255, 0.5);
    padding: 1rem;
    border-radius: 1rem;
    text-align: center;
  }
}
